The certificate and private key in this folder are self-signed.
To use badrat over HTTPS, replace the certificate and private key with your own.
Make sure to specify --ssl when starting badrat otherwise it will default to http.
